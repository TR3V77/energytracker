image: python:3.12-slim

pipelines:
  default:
    - step:
        name: Run backend tests
        caches:
          - pip
        script:
          - pip install -r backend/requirements.txt
          - cd backend && pytest

  pull-requests:
    main:
      - step:
          name: Run backend tests
          caches:
            - pip
          script:
            - pip install -r backend/requirements.txt
            - cd backend && pytest

  branches:
    main:
      - step:
          name: Run backend tests
          caches:
            - pip
          script:
            - pip install -r backend/requirements.txt
            - cd backend && pytest
